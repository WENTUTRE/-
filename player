#ifndef H_H
#define H_H

#include <string>
using namespace std;

class player
{
private:
	string name;
	string action;
	int point;

public:
	player();
	player(string, int);
	void setName(string);
	void setPoint(int);
	string getName();
	int getPoint();
	string action();
	
};
#endif
